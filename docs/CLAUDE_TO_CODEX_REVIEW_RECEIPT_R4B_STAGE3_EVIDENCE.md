## Findings

**F1 — Fresh-profile tarball install and bundle activation are genuinely proven (PASS).** `artifacts/stage-3/b638cdd/dsh-home/profiles/effect-doctor-stage3-b638cdd/package.json` is a private, isolated profile manifest whose only product dependency is `file:D://knowledgeBase//dsh-effect-doctor//artifacts//stage-3//b638cdd//dsh-effect-doctor-0.1.0.tgz` and whose `dsh.profile.bundles` contains `@deepseek-ai/dsh-base` and `dsh-effect-doctor`. `scripts/dsh-real-gate.ts:236-242` fails closed on either condition. The installed entry `.../node_modules/dsh-effect-doctor/dist/src/dsh-plugin.js` exists and is byte-identical in content to reviewed `src/dsh-plugin.ts`. Profile name and `DSH_HOME` are both scoped to the artifact tree; no active profile is referenced anywhere in the evidence.

**F2 — DSH bundle composition evidence is real, not synthesized.** `artifacts/stage-3/b638cdd/runtime-gate/dump-config.txt` is a 321-line effective entry list ending with `# == dsh-effect-doctor` / `- id: effect-doctor-receipt` / `name: dsh-effect-doctor/dsh-plugin` / `receiptPath: !!js ctx.dshHomePath('effect-doctor/latest/receipt.json')`, i.e. the bundle patch applied after `@deepseek-ai/dsh-base`. The `!!js` dialect matches the allowed DSH file `packages/boot/app-boot/lib/index.js:15-28,42-46`, whose own comments state the dialect and `applyEntryPatches` are shared by mounting and by `dsh --dump-config`, so a dump cannot drift from what boots. The gate requires all three substrings and rejects any child stderr (`dsh-real-gate.ts:258-263`).

**F3 — app-boot / Loader / ToolRuntime usage is real, and disposal proof is sound.** `boot(binName, absoluteConfigPath)` matches `app-boot/lib/index.js:1166`. `ctx.get('tools')` returns the real `ToolRuntime` (`packages/core/tools/lib/index.js:2546,2585` — `super(ctx, "tools")`), whose `register()` asserts the schema and returns a fiber-scoped effect disposer (`:2755-2763`), and whose `get(name)` resolves the global visible view (`:2872-2874`). Disposal targets only `include:effect-doctor` (`dsh-real-gate.ts:315-321`); `uid` semantics are confirmed by `vendor/cordis/lib/index.js:1011-1012,1077` (`uid = null` on dispose), and the gate asserts `uid !== null` *before* disposing, so the non-null → null transition is real rather than vacuous. Crucially, `tools.get(TOOL_NAME)` is still *callable* after the doctor fiber is disposed, which independently demonstrates the sibling `tools` fiber survived — precise, not tree-wide, disposal. The whole test root is then disposed in `finally` (`:322-324`).

**F4 — Corroborating side-evidence of a live Loader.** The preserved `runtime-gate/loader.cordis.yml` is *not* byte-identical to what the gate authors at `dsh-real-gate.ts:268-282`: it uses folded `>-` scalars and carries `disabled: true` on the `effect-doctor` entry only. That is Loader write-back after the entry fiber was disposed, and it independently corroborates both a real Loader and entry-scoped disposal. Consequence to record: `loaderConfigSha256` is the hash of the post-run mutated file, not of the authored input, so a reproducer that writes the same input bytes will not reproduce that hash. This is undocumented in `docs/STAGE_3_DSH_GATE.md` (table row "Minimal Loader configuration").

**F5 — Output/render consistency is strong; the schema row is weak and reads stronger than it is.** `receiptMatches` is exact `JSON.stringify` deep equality against a separately parsed receipt, and `renderMatches` requires exactly one `{type:'text'}` part equal to `renderReceiptSummary(expected)` (`dsh-real-gate.ts:304-313`) — both fail closed. However the registered output schema is literally `{}` (`src/dsh-plugin.ts:70`, and the installed `dist/src/dsh-plugin.js:34`). Per `packages/core/tools/lib/index.js:426,441-443`, a node without `type` only checks "lossless JSON value". So `outputSchemaValid` proves the tool value is JSON-safe, not that it conforms to `ReceiptSummary`. `docs/STAGE_3_DSH_GATE.md:31` ("Tool output satisfied registered JSON Schema | PASS") and `docs/release-report.md:22` are literally true — real validator, real registered schema — but overstate the discriminating power. The substantive shape guarantee comes from `receiptMatches` plus the strict `parseReceipt` in `src/receipt-viewer.ts:131-218`, not from the schema.

**F6 — "Independently parsed receipt" means separate read, not independent implementation.** The gate's `expected` comes from `readReceiptSummary` imported from `../src/receipt-viewer.js` (`dsh-real-gate.ts:7,287`) — the same module the installed plugin uses, from a different copy. So the comparison proves the installed tarball artifact behaves identically to the reviewed source build and reads the configured receipt path; it is not cross-implementation validation. The notice's phrase "独立 receipt parser/render" and the doc row "matched independently parsed receipt" should be read in that narrower sense.

**F7 — Tool dispatch pipeline was not exercised.** `tool.execute({}, {signal})` is invoked directly on the definition, bypassing `ToolRuntime` dispatch (`createSuccessResult` / `snapshotToolValue` / `deepFreeze` / `ToolOutputError` / timeout policy, `tools/lib/index.js:3397-3427`). Neither `STAGE_3_DSH_GATE.md` nor `release-report.md` claims a model-facing dispatch, so this is a scope boundary rather than an overstatement, but it is currently undeclared in `Limits`.

**F8 — Fail-closed audit of the gate: no bypass found.** All nine required files are `stat`-asserted as files (`:222-234`); `createFreshDirectory` throws `Stage 3 output directory already exists` and only swallows `ENOENT` (`:128-136`); `parseArgs` enforces `--name value` pairs, no duplicates, no unknown args, absolute paths and a `D:` root on win32 (`:80-117`); the child runs under an explicit env allowlist with `DSH_PERMISSION_MODE=read-only` and no credential variables (`:142-169`); `execFile` is bounded by `timeout` and `maxBuffer` and any non-zero exit/timeout rejects (`:171-192`); non-empty stderr fails for both the dump and `git rev-parse` (`:200,258`); the summary is written atomically via tmp+rename *after* every check, so a failing run writes no summary at all and exits 1 (`:366-367,392-397`). The four literal `true` booleans (`profileDependency`, `bundleActivated`, `dumpConfigMatched`, `loaderBooted`) are unreachable unless the corresponding throws did not fire, so they are sound though non-expressive.

**F9 — Summary/docs/hash self-consistency holds where statically checkable.** All six hashes and the profile, both commits, the four receipt fields and the two limitation strings in `gate-summary.json` match `docs/STAGE_3_DSH_GATE.md:7-11,44-49` and the Baseline in the notice, verbatim. `gate-summary.json` `receipt` = `{1.0.0, clean, PASS, 0}` matches the raw `dsh-home/effect-doctor/latest/receipt.json:2,10,376,377`. Independently, that receipt's `runtime.sourceHash` `1729cdbf…e751c5` equals the summary's `cordisRuntimeSha256`, so one of the six hashes is corroborated by an artifact produced by a different code path (the engine) at a different time. `gate-summary.json` contains no key, credential, secret, password, cookie, or authorization property name — the doc's audit claim (`:51`) is scoped to the summary and is accurate. Only `runtime-gate/{dump-config.txt,loader.cordis.yml,gate-summary.json}` exist; no `.tmp` residue.

**F10 — No-Web classification is substantively correct, with one wording imprecision.** `package.json:12-21` exports only `.` and `./dsh-plugin`; no `bin`-served surface, no client entry; `src/` contains no `.tsx/.jsx/.css/.html`, no `ctx.slot`, no client/style registration. So `NOT_APPLICABLE_NO_WEB_SURFACE` for client HTTP, style-node disposal, browser console/page/request errors, screenshots and GIFs is honest, and no substitute screenshot exists. Imprecision: `STAGE_3_DSH_GATE.md:57` / `release-report.md:24` say "no client export, browser bundle, slot, style, or Web surface", yet `src/report.ts:70` emits an inline `<style>` block into a standalone `receipt.html` (present at `dsh-home/effect-doctor/latest/receipt.html`). That HTML is a never-served CLI evidence artifact with no DSH client slot, so the classification stands; the phrase "or style" should be narrowed to "no DSH client style registration".

**F11 — Other truthfulness checks pass.** `PASS_AFTER_CHANGES` is justified by six enumerated pre-pass corrections (`release-report.md:33-38`), and the historical `cd0d0f7`/`004fe47` runs are explicitly retained as non-final. PR prohibition is stated consistently in three places (`STAGE_3_DSH_GATE.md:63`, `release-report.md:27,47,52`, `release-evidence.md:39-41`) and never claims Claude's `GO`, aggregate approval, or eligibility. `secret persisted: false` is consistent with the env allowlist, with `summarizeReceipt` dropping `runtime.modulePath` from the model-visible projection (`src/receipt-viewer.ts:236-245`), and with the absence of secret material in all read artifacts. Recorded for completeness, not as a defect: `dump-config.txt:294,72-73` contains the env-var *name* `DEEPSEEK_API_KEY` and a default telemetry endpoint — names and URLs from the DSH base bundle, no secret values.

## Actions Executed and Not Executed

Executed (read-only, this session only): `Read` of `docs/CODEX_TO_CLAUDE_REVIEW_NOTICE_R4B_STAGE3_EVIDENCE.md`, `scripts/dsh-real-gate.ts`, `docs/STAGE_3_DSH_GATE.md`, `docs/release-report.md`, `docs/release-evidence.md`, `package.json`, `artifacts/stage-3/b638cdd/runtime-gate/{gate-summary.json,dump-config.txt,loader.cordis.yml}`, `artifacts/stage-3/b638cdd/dsh-home/profiles/effect-doctor-stage3-b638cdd/package.json`, and the installed `.../node_modules/dsh-effect-doctor/dist/src/dsh-plugin.js`; `Read`/`Grep` of the five allowed DSH support files (`app-boot`, `core/tools`, `vendor/cordis`; `core/system-prompt` and root `package.json` were not needed and were not opened); `Glob` inventory of `artifacts/stage-3/b638cdd/**` and `src/**`.

Executed beyond the notice's enumerated product-file list, disclosed here: `Read` of `src/dsh-plugin.ts` and `src/receipt-viewer.ts` (required to judge the `output.schema` and render/parser claims in F5–F6), `Read` of `artifacts/stage-3/b638cdd/dsh-home/effect-doctor/latest/receipt.json` (required to check summary↔raw-receipt consistency in F9), and one `Grep` over `src/` for client/style markers (F10). All were read-only, in-repository, and non-mutating.

Not executed: no Bash or PowerShell; no `npm pack`, `pnpm dsh plugin add`, `dsh-real-gate.js`, `npm run verify`, or any hashing; no Write/Edit of any repository file (including the target receipt path `docs/CLAUDE_TO_CODEX_REVIEW_RECEIPT_R4B_STAGE3_EVIDENCE.md`, which this session leaves absent — the receipt is returned as text only); no commit, push, deploy, service, or scheduled task; no Codex, Claude sub-session, subagent, Agent tool, or other model; no WebFetch/WebSearch; no credential, production, active-profile, or session/model/tool-history access; no read of `D:\knowledgeBase\dsh-branch-review`; no external project file outside the five allowed DSH paths; no permission bypass.

## Review Scope

Final `b638cdd` Stage 3 gate implementation and its saved raw evidence only: `scripts/dsh-real-gate.ts` as committed, the four preserved runtime-gate/profile artifacts, the receipt they reference, and the truthfulness/self-consistency of `docs/STAGE_3_DSH_GATE.md`, `docs/release-report.md`, and `docs/release-evidence.md` against those artifacts and against the five allowed DSH built entries at DSH commit `47f9438`. Engine and receipt-viewer implementations were reviewed in R3/R4A and are not re-reviewed here (they were read only to adjudicate Stage 3 evidence claims). Out of scope: aggregate final decision, release, PR, repository-age eligibility, security sign-off, human approval, Stage 1/2 re-verification, and any live execution.

## Evidence Gaps

1. No hash, tarball unpack, or command can be recomputed in this read-only mode: the six artifact hashes, the summary SHA `42236dd8…48e63`, the tarball SHA and its 27-file content contract, both git commits, and the `npm run verify` counts (17/17, 4/4, seven receipts, zero skips) rest on Codex's independent execution and on the notice's Baseline. This is the notice's declared Known Gap.
2. The "existing evidence directory rejected / exit 1" row (`STAGE_3_DSH_GATE.md:36`) has no raw artifact by design (a rejected run writes nothing). The code path at `dsh-real-gate.ts:128-136,244` clearly enforces it, but the observed exit code and the unchanged summary hash are Codex-attested only.
3. `apps/cli/src/bin.ts` is outside the allowed read list, so `--dump-config`'s exact semantics were confirmed only indirectly, via `app-boot/lib/index.js:25,45` stating that dumping and mounting share the same dialect and patch semantics.
4. `cordis-plugin-loader` is outside the allowed read list, so the mechanism that produced `disabled: true` in the preserved `loader.cordis.yml` (F4) is inferred from the artifact, not read from Loader source.
5. Chain of custody from tarball bytes to the installed `dsh-plugin.js` is established by the profile manifest's exact `file:` tarball path plus two recorded hashes, not by unpacking; the gate never re-derives the installed file from the tarball.
6. `npm run verify` (`package.json:46`) does not include `gate:dsh` (only `verify:dsh-gate`, i.e. `--help`), so the Stage 3 gate is a manual step; no CI or scripted invocation guarantees it re-runs.

## Residual Risks

1. `sourceCommit` is `git rev-parse HEAD` of the product repo (`dsh-real-gate.ts:194-204,330`) with no clean-worktree check, so the summary records the commit that was checked out, not proof that the built/packed artifacts correspond to that commit's tracked source. Uncommitted drift would be recorded as `b638cdd`.
2. `outputSchemaValid` is near-vacuous while `output.schema` stays `{}` (F5): a future output-shape regression would be caught by `receiptMatches`, but the schema row would keep reading `PASS`. Recommend either a real `ReceiptSummary` output schema or a doc footnote that the registered schema is intentionally unconstrained.
3. `loaderConfigSha256` pins Loader-mutated bytes (F4), so that one hash is not reproducible from the gate's authored input; an honest reproducer may see a mismatch and misread it as tampering.
4. A throw from `await ctx?.fiber.dispose()` in `finally` (`:322-324`) would replace the primary failure message. The run still exits non-zero, but the diagnostic could be lost — the same precedence class that was fixed in the engine at `b638cdd`.
5. `execFile` timeout kills only the direct child (no `detached`/tree kill), so on win32 a grandchild of the `tsx` dump-config process could in principle outlive a timeout.
6. `process.env.DSH_HOME` is mutated in-process and never restored (`:284`); harmless for a one-shot gate, but it makes the gate unsafe to call twice in one process.
7. The booted tree is three entries (system-prompt, tools, installed plugin) with an explicit `receiptPath`, not the dumped 100+ entry profile composition with `ctx.dshHomePath(...)`. The docs do say "minimal", and composition is covered separately by F2, but registration under the full base bundle is asserted only by the dump, never booted.
8. Wording precision items to fix before any aggregate submission, none of which change a gate outcome: "no ... style" vs. `src/report.ts:70` (F10), "independently parsed" (F6), the undeclared direct-`execute` boundary (F7), and the post-run nature of the Loader-config hash (F4).

FINAL_DECISION: GO
